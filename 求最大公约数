#include<stdio.h>
int main()
{
  int a,b,c,i;
  printf("请输入两个数字：");
  scanf("%d%d",&a,&b);
  while(a%b!=0)
  {
    c=a%b;
    a=b;
    b=c;
  }
  if(b>0)
  i=b;
  else
  i=-b;
  printf("最大公约数为%d\n",i);
  return 0;
}
